package exam01;

public class Sample01 {

	public static void main(String[] args) {   //프로그램의 시작은 MAIN에서 시작한다.
		 Person p =new  Person();
		 //person 이라는 걸 통해서 new 만들어라 변수명(변수공간) p에 저장해라!
		 //인스턴스로 변화한다. NEW Person을 P에 넣는것. 
		//-클래스(제품설계도)를 통해서 객체(제품)를 만드는 과정!!(인스턴스화)
		//(객체=인스턴스)라고 부름. 클래스가 인스턴스로 바꾸다. 변환한다.
		 
     	 p.pNO ="";                     
		 //객체를 참조할 수 있는 참조값이 P에 있음.//인스턴스는 (HEAP메모리-배열과 동일하게)에 있다.
     	
	}

}
